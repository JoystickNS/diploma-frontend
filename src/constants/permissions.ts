export enum RoleName {
  Admin = "Admin",
  Teacher = "Teacher",
  Manager = "Manager",
}

export enum ActionName {
  Create = "Create",
  Read = "Read",
  Update = "Update",
  Delete = "Delete",
}

export enum SubjectName {
  Admin = "Admin",
  Journal = "Journal",
  Report = "Report",
}
